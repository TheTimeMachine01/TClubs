# K8s/secrets.yaml - Template for secrets (created via scripts/create-secrets.sh from .env)
# Do not commit actual secrets here. Use the script to create secrets from .env file.
apiVersion: v1
kind: Secret
metadata:
  name: tclubs-secrets
type: Opaque
stringData:
  # Secrets are created from .env file using scripts/create-secrets.sh
  # JWT_SECRET: ""
  # JWT_EXPIRATION_TIME: ""
  # JWT_REFRESH_EXPIRATION_TIME: ""
